export default function SetupScreen() {
    return (
        <div className='start'>
          <h2>Welcome to the react quiz!</h2>
          <h3>
            hello
          </h3>
          <button className="btn btn-ui" onClick={() => {}}>Let's start</button>
        </div>
      );
}